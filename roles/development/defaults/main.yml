---
# Development role: Default variables
# nodejs_version and composer_version are defined in inventory/group_vars/all.yml

# Node.js configuration
install_nodejs: true

# Laravel configuration
install_laravel: true
