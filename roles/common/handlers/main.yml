---
# Common role: Handlers

- name: Restart ssh
  ansible.builtin.service:
    name: "{{ common_ssh_service_name }}"
    state: restarted

- name: Restart rsyslog
  ansible.builtin.service:
    name: rsyslog
    state: restarted
