---
# GoProxy role: Handlers

- name: Restart tor
  ansible.builtin.service:
    name: Tor
    state: restarted

- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart goproxy
  ansible.builtin.service:
    name: Goproxy
    state: restarted
